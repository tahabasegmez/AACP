<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Android 13+ bildirim izni.

      Oynatma bildirimi (kilit ekranı kontrolleri) ve ileride push bunu ister.
      İzin ÇALIŞMA ZAMANINDA da sorulmalıdır; yalnızca burada bildirmek yetmez.

      Not: oynatma için gereken WAKE_LOCK, FOREGROUND_SERVICE ve
      FOREGROUND_SERVICE_MEDIA_PLAYBACK izinleriyle MusicService bildirimini
      react-native-track-player kendi manifestinde tanımlar; manifest birleştirme
      onları buraya taşır, tekrar yazmaya gerek yoktur.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      react-native-blob-util kendi manifestinde harici depolama izinleri
      bildiriyor. BİZ KULLANMIYORUZ: indirilen bölümler `DocumentDir`,
      kapak önbelleği `CacheDir` altında — ikisi de uygulamaya özel alan.

      İhtiyaç duyulmayan bu izinler sevk edilirse Google Play gerekçe ister,
      kullanıcı yükleme ekranında "dosyalarınıza erişim" görür ve Android 10+
      kapsamlı depolama nedeniyle izin zaten işlevsizdir. Bu yüzden birleşmiş
      manifestten çıkarılıyorlar.

      DOWNLOAD_WITHOUT_NOTIFICATION yalnızca Android DownloadManager yolu
      (`addAndroidDownloads`) kullanıldığında gerekir; o yolu kullanmıyoruz.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" tools:node="remove" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
